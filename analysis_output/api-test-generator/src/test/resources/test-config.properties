# Test configuration – override values via environment variables or system properties
# See TestConfig.java for supported overrides.

api.base.url=http://localhost:8080
api.version=/api/v1
api.timeout.connection=5000
api.timeout.read=10000
